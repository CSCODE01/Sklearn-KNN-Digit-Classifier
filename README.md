# Sklearn-KNN-Digit-Classifier
Implementation of a Digit Classifier using the MNIST-like "Digits" dataset. This project demonstrates the full ML pipeline: cleaning, scaling, model training, and evaluation with a Confusion Matrix.
